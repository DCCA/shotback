# Proposal: FIREHOSE Compliance Alignment

## Why
Current `.docs/doing` structure does not follow the required per-change folder format in `FIREHOSE.md`.

## Scope
- Create a compliant active change folder in `.docs/doing/`.
- Add `proposal.md`, `spec.md`, `design.md`, and `tasks.md`.
- Remove legacy single-file tracker format.

## Out of Scope
- Product feature changes.
- Runtime code updates.
