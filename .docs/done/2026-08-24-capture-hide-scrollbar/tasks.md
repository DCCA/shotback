# Tasks

- [x] 1. Extend the e2e stitched-capture test to sample the right edge (RED)
- [x] 2. `content.ts`: `hideScrollbars`/`showScrollbars`, tag the scroller,
      hide before measuring in `SB_GET_PAGE_METRICS`, restore in
      `SB_RESTORE_SCROLL` and `SB_CAPTURE_END` (GREEN)
- [x] 3. `npm run test:e2e` + `npm run check` + `npm run format:check` green
- [x] 4. Docs: CLAUDE.md capture-flow note, this change folder
- [x] 5. Commit, push, PR
