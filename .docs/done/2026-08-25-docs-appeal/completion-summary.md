# Completion summary

## What changed

- README.md rewritten for appeal: a one-sentence pitch ("annotated screenshots
  your coding agent can actually act on"), a real hero screenshot of the editor
  generated from the built extension (light and dark via a `<picture>` element
  that follows the reader's GitHub theme), features grouped under Capture /
  Annotate / Protect / Hand off with one line each, and the deep content
  (Claude Code handoff, prompt detail, diagnostics honesty, privacy posture)
  kept but tightened. Stale content fixed: project structure now lists the real
  directories (background/content scripts, e2e tests), the keyboard notes
  describe Escape-discard and keyboard shape placement, and the dev-command
  table includes `npm run check` and `npm run test:e2e`.
- docs/media/hero-light.png and hero-dark.png added (about 170 KB each),
  generated by scripting the real unpacked extension in Chromium: a fixture
  pricing page captured, annotated with a box, highlight and arrow (notes and
  element descriptors resolved live) and an API key redacted.
- CONTRIBUTING.md gains the e2e command and when to run it.
- SECURITY.md, PRIVACY.md, AGENTS.md, FIREHOSE.md deliberately untouched.

## Risks

None functional - docs and images only. The screenshots will drift as the UI
evolves; regenerate them the same way (script the unpacked extension) rather
than hand-editing.

## Follow-ups

- Replace the Web Store placeholder with the listing link when published.
